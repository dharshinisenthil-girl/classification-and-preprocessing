# classification-and-preprocessing
This project demonstrates the importance of feature scaling in machine learning. It includes implementations of Standardization (Z-score normalization) and Min-Max Scaling, showing how scaling improves model performance and convergence, especially for distance-based and gradient-based algorithms.
